describe('Panier ZotoShop', () => {
    beforeEach(() => {
        cy.visit("/");
    });
    it('should add product to cart', () => {
        cy.log("T000: implementer ce test");
        
    });
    it('should update product quantity in cart', () => {
        cy.log("T000: implementer ce test");
        
    });
    it('should renove product from cart', () => {
        cy.log("T000: implementer ce test");
        cy.log("T000: implementer ce test2");
        
    });
    
});